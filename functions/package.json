{
  "name": "swap-barter-functions",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "engines": { "node": "18" },
  "main": "lib/index.js",
  "dependencies": {
    "axios": "^1.7.2",
    "firebase-admin": "^12.3.0",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.2"
  },
  "private": true
}
